using UnityEngine;

public class onclick_event : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is create

    public void Dupa()
    {
        Debug.Log("DUPA");
    }
}
